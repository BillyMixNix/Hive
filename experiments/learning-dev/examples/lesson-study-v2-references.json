{
  "current_input_00": {
    "component_72ec0f84.py": "class Quote_0:\n    def __init__(self, opening_rate):\n        self.opening_rate = opening_rate\n\n    def amount(self, units, live_rate, fee):\n        subtotal = units * live_rate\n        return subtotal + fee\n",
    "test_visible.py": "from component_72ec0f84 import Quote_0\n\ndef test_visible():\n    q = Quote_0(11)\n    assert q.amount(3, 16, 2) == 3 * 16 + 2\n    assert q.amount(0, 18, 6) == 6\n    assert q.amount(2, 12, 0) == 2 * 12\n"
  },
  "current_input_01": {
    "component_1cd3fedc.py": "class Allowance_1:\n    def __init__(self, opening_balance):\n        self.opening_balance = opening_balance\n\n    def remaining(self, requested, current_balance):\n        available = current_balance - requested\n        return max(0, available)\n",
    "test_visible.py": "from component_1cd3fedc import Allowance_1\n\ndef test_visible():\n    a = Allowance_1(18)\n    assert a.remaining(3, 27) == 24\n    assert a.remaining(28, 2) == 0\n    assert a.remaining(0, 20) == 20\n"
  },
  "current_input_02": {
    "component_7370ed07.py": "class Converter_2:\n    def __init__(self, initial_width):\n        self.initial_width = initial_width\n\n    def buckets(self, volume, current_width):\n        complete = volume // current_width\n        return complete\n",
    "test_visible.py": "from component_7370ed07 import Converter_2\n\ndef test_visible():\n    c = Converter_2(25)\n    assert c.buckets(227, 3) == (227 // 3)\n    assert c.buckets(0, 5) == 0\n    assert c.buckets(125, 27) == (125 // 27)\n"
  },
  "current_input_03": {
    "component_8ff6f82e.py": "class Quote_3:\n    def __init__(self, opening_rate):\n        self.opening_rate = opening_rate\n\n    def amount(self, units, live_rate, fee):\n        subtotal = units * live_rate\n        return subtotal + fee\n",
    "test_visible.py": "from component_8ff6f82e import Quote_3\n\ndef test_visible():\n    q = Quote_3(32)\n    assert q.amount(3, 37, 2) == 3 * 37 + 2\n    assert q.amount(0, 39, 6) == 6\n    assert q.amount(2, 33, 0) == 2 * 33\n"
  },
  "current_input_04": {
    "component_00ae25c2.py": "class Allowance_4:\n    def __init__(self, opening_balance):\n        self.opening_balance = opening_balance\n\n    def remaining(self, requested, current_balance):\n        available = current_balance - requested\n        return max(0, available)\n",
    "test_visible.py": "from component_00ae25c2 import Allowance_4\n\ndef test_visible():\n    a = Allowance_4(39)\n    assert a.remaining(3, 48) == 45\n    assert a.remaining(49, 2) == 0\n    assert a.remaining(0, 41) == 41\n"
  },
  "current_input_05": {
    "component_213184c1.py": "class Converter_5:\n    def __init__(self, initial_width):\n        self.initial_width = initial_width\n\n    def buckets(self, volume, current_width):\n        complete = volume // current_width\n        return complete\n",
    "test_visible.py": "from component_213184c1 import Converter_5\n\ndef test_visible():\n    c = Converter_5(46)\n    assert c.buckets(416, 3) == (416 // 3)\n    assert c.buckets(0, 5) == 0\n    assert c.buckets(230, 48) == (230 // 48)\n"
  },
  "current_input_06": {
    "component_c34c9bbc.py": "class Quote_6:\n    def __init__(self, opening_rate):\n        self.opening_rate = opening_rate\n\n    def amount(self, units, live_rate, fee):\n        subtotal = units * live_rate\n        return subtotal + fee\n",
    "test_visible.py": "from component_c34c9bbc import Quote_6\n\ndef test_visible():\n    q = Quote_6(53)\n    assert q.amount(3, 58, 2) == 3 * 58 + 2\n    assert q.amount(0, 60, 6) == 6\n    assert q.amount(2, 54, 0) == 2 * 54\n"
  },
  "current_input_07": {
    "component_95636221.py": "class Allowance_7:\n    def __init__(self, opening_balance):\n        self.opening_balance = opening_balance\n\n    def remaining(self, requested, current_balance):\n        available = current_balance - requested\n        return max(0, available)\n",
    "test_visible.py": "from component_95636221 import Allowance_7\n\ndef test_visible():\n    a = Allowance_7(60)\n    assert a.remaining(3, 69) == 66\n    assert a.remaining(70, 2) == 0\n    assert a.remaining(0, 62) == 62\n"
  },
  "current_input_08": {
    "component_40c3a4c9.py": "class Converter_8:\n    def __init__(self, initial_width):\n        self.initial_width = initial_width\n\n    def buckets(self, volume, current_width):\n        complete = volume // current_width\n        return complete\n",
    "test_visible.py": "from component_40c3a4c9 import Converter_8\n\ndef test_visible():\n    c = Converter_8(67)\n    assert c.buckets(605, 3) == (605 // 3)\n    assert c.buckets(0, 5) == 0\n    assert c.buckets(335, 69) == (335 // 69)\n"
  },
  "current_input_09": {
    "component_2b6eef9f.py": "class Quote_9:\n    def __init__(self, opening_rate):\n        self.opening_rate = opening_rate\n\n    def amount(self, units, live_rate, fee):\n        subtotal = units * live_rate\n        return subtotal + fee\n",
    "test_visible.py": "from component_2b6eef9f import Quote_9\n\ndef test_visible():\n    q = Quote_9(74)\n    assert q.amount(3, 79, 2) == 3 * 79 + 2\n    assert q.amount(0, 81, 6) == 6\n    assert q.amount(2, 75, 0) == 2 * 75\n"
  },
  "current_input_10": {
    "component_85dcb117.py": "class Allowance_10:\n    def __init__(self, opening_balance):\n        self.opening_balance = opening_balance\n\n    def remaining(self, requested, current_balance):\n        available = current_balance - requested\n        return max(0, available)\n",
    "test_visible.py": "from component_85dcb117 import Allowance_10\n\ndef test_visible():\n    a = Allowance_10(81)\n    assert a.remaining(3, 90) == 87\n    assert a.remaining(91, 2) == 0\n    assert a.remaining(0, 83) == 83\n"
  },
  "current_input_11": {
    "component_254045ce.py": "class Converter_11:\n    def __init__(self, initial_width):\n        self.initial_width = initial_width\n\n    def buckets(self, volume, current_width):\n        complete = volume // current_width\n        return complete\n",
    "test_visible.py": "from component_254045ce import Converter_11\n\ndef test_visible():\n    c = Converter_11(88)\n    assert c.buckets(794, 3) == (794 // 3)\n    assert c.buckets(0, 5) == 0\n    assert c.buckets(440, 90) == (440 // 90)\n"
  },
  "current_input_12": {
    "component_83933ff6.py": "class Quote_12:\n    def __init__(self, opening_rate):\n        self.opening_rate = opening_rate\n\n    def amount(self, units, live_rate, fee):\n        subtotal = units * live_rate\n        return subtotal + fee\n",
    "test_visible.py": "from component_83933ff6 import Quote_12\n\ndef test_visible():\n    q = Quote_12(95)\n    assert q.amount(3, 100, 2) == 3 * 100 + 2\n    assert q.amount(0, 102, 6) == 6\n    assert q.amount(2, 96, 0) == 2 * 96\n"
  },
  "current_input_13": {
    "component_85e0afc1.py": "class Allowance_13:\n    def __init__(self, opening_balance):\n        self.opening_balance = opening_balance\n\n    def remaining(self, requested, current_balance):\n        available = current_balance - requested\n        return max(0, available)\n",
    "test_visible.py": "from component_85e0afc1 import Allowance_13\n\ndef test_visible():\n    a = Allowance_13(102)\n    assert a.remaining(3, 111) == 108\n    assert a.remaining(112, 2) == 0\n    assert a.remaining(0, 104) == 104\n"
  },
  "current_input_14": {
    "component_83ebf2e8.py": "class Converter_14:\n    def __init__(self, initial_width):\n        self.initial_width = initial_width\n\n    def buckets(self, volume, current_width):\n        complete = volume // current_width\n        return complete\n",
    "test_visible.py": "from component_83ebf2e8 import Converter_14\n\ndef test_visible():\n    c = Converter_14(109)\n    assert c.buckets(983, 3) == (983 // 3)\n    assert c.buckets(0, 5) == 0\n    assert c.buckets(545, 111) == (545 // 111)\n"
  },
  "paired_sort_00": {
    "component_641e98a7.py": "def arrange_0(labels, payloads):\n    if len(labels) != len(payloads):\n        raise ValueError('length mismatch')\n    paired = sorted(zip(labels, payloads), key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_641e98a7 import arrange_0\n\ndef test_visible():\n    assert arrange_0([13, 11, 12], ['b', 'z', 'a']) == [(11, 'z'), (12, 'a'), (13, 'b')]\n    assert arrange_0([11, 11], ['z', 'a']) == [(11, 'z'), (11, 'a')]\n    assert arrange_0([], []) == []\n"
  },
  "paired_sort_01": {
    "component_670d7acd.py": "def arrange_1(records):\n    ranks = sorted(row['rank'] for row in records)\n    selected = sorted(records, key=lambda row: row['rank'])\n    return selected\n",
    "test_visible.py": "from component_670d7acd import arrange_1\n\ndef test_visible():\n    rows = [{'rank': 19, 'value': 'b'}, {'rank': 18, 'value': 'z'}, {'rank': 18, 'value': 'a'}]\n    assert arrange_1(rows) == [rows[1], rows[2], rows[0]]\n    assert rows[0]['rank'] == 19\n    assert arrange_1([]) == []\n"
  },
  "paired_sort_02": {
    "component_252938c6.py": "def arrange_2(records):\n    keys = sorted((row[0], row[1]) for row in records)\n    paired = sorted([((row[0], row[1]), row[2]) for row in records], key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_252938c6 import arrange_2\n\ndef test_visible():\n    rows = [('b', 25, 'x'), ('a', 26, 'z'), ('a', 25, 'm'), ('a', 25, 'a')]\n    assert arrange_2(rows) == [(('a', 25), 'm'), (('a', 25), 'a'), (('a', 26), 'z'), (('b', 25), 'x')]\n    assert arrange_2([]) == []\n"
  },
  "paired_sort_03": {
    "component_e7f04852.py": "def arrange_3(labels, payloads):\n    if len(labels) != len(payloads):\n        raise ValueError('length mismatch')\n    paired = sorted(zip(labels, payloads), key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_e7f04852 import arrange_3\n\ndef test_visible():\n    assert arrange_3([34, 32, 33], ['b', 'z', 'a']) == [(32, 'z'), (33, 'a'), (34, 'b')]\n    assert arrange_3([32, 32], ['z', 'a']) == [(32, 'z'), (32, 'a')]\n    assert arrange_3([], []) == []\n"
  },
  "paired_sort_04": {
    "component_0aa6a844.py": "def arrange_4(records):\n    ranks = sorted(row['rank'] for row in records)\n    selected = sorted(records, key=lambda row: row['rank'])\n    return selected\n",
    "test_visible.py": "from component_0aa6a844 import arrange_4\n\ndef test_visible():\n    rows = [{'rank': 40, 'value': 'b'}, {'rank': 39, 'value': 'z'}, {'rank': 39, 'value': 'a'}]\n    assert arrange_4(rows) == [rows[1], rows[2], rows[0]]\n    assert rows[0]['rank'] == 40\n    assert arrange_4([]) == []\n"
  },
  "paired_sort_05": {
    "component_57655270.py": "def arrange_5(records):\n    keys = sorted((row[0], row[1]) for row in records)\n    paired = sorted([((row[0], row[1]), row[2]) for row in records], key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_57655270 import arrange_5\n\ndef test_visible():\n    rows = [('b', 46, 'x'), ('a', 47, 'z'), ('a', 46, 'm'), ('a', 46, 'a')]\n    assert arrange_5(rows) == [(('a', 46), 'm'), (('a', 46), 'a'), (('a', 47), 'z'), (('b', 46), 'x')]\n    assert arrange_5([]) == []\n"
  },
  "paired_sort_06": {
    "component_93790b8e.py": "def arrange_6(labels, payloads):\n    if len(labels) != len(payloads):\n        raise ValueError('length mismatch')\n    paired = sorted(zip(labels, payloads), key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_93790b8e import arrange_6\n\ndef test_visible():\n    assert arrange_6([55, 53, 54], ['b', 'z', 'a']) == [(53, 'z'), (54, 'a'), (55, 'b')]\n    assert arrange_6([53, 53], ['z', 'a']) == [(53, 'z'), (53, 'a')]\n    assert arrange_6([], []) == []\n"
  },
  "paired_sort_07": {
    "component_1a1cb575.py": "def arrange_7(records):\n    ranks = sorted(row['rank'] for row in records)\n    selected = sorted(records, key=lambda row: row['rank'])\n    return selected\n",
    "test_visible.py": "from component_1a1cb575 import arrange_7\n\ndef test_visible():\n    rows = [{'rank': 61, 'value': 'b'}, {'rank': 60, 'value': 'z'}, {'rank': 60, 'value': 'a'}]\n    assert arrange_7(rows) == [rows[1], rows[2], rows[0]]\n    assert rows[0]['rank'] == 61\n    assert arrange_7([]) == []\n"
  },
  "paired_sort_08": {
    "component_668dc700.py": "def arrange_8(records):\n    keys = sorted((row[0], row[1]) for row in records)\n    paired = sorted([((row[0], row[1]), row[2]) for row in records], key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_668dc700 import arrange_8\n\ndef test_visible():\n    rows = [('b', 67, 'x'), ('a', 68, 'z'), ('a', 67, 'm'), ('a', 67, 'a')]\n    assert arrange_8(rows) == [(('a', 67), 'm'), (('a', 67), 'a'), (('a', 68), 'z'), (('b', 67), 'x')]\n    assert arrange_8([]) == []\n"
  },
  "paired_sort_09": {
    "component_ca50f46f.py": "def arrange_9(labels, payloads):\n    if len(labels) != len(payloads):\n        raise ValueError('length mismatch')\n    paired = sorted(zip(labels, payloads), key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_ca50f46f import arrange_9\n\ndef test_visible():\n    assert arrange_9([76, 74, 75], ['b', 'z', 'a']) == [(74, 'z'), (75, 'a'), (76, 'b')]\n    assert arrange_9([74, 74], ['z', 'a']) == [(74, 'z'), (74, 'a')]\n    assert arrange_9([], []) == []\n"
  },
  "paired_sort_10": {
    "component_820f19e2.py": "def arrange_10(records):\n    ranks = sorted(row['rank'] for row in records)\n    selected = sorted(records, key=lambda row: row['rank'])\n    return selected\n",
    "test_visible.py": "from component_820f19e2 import arrange_10\n\ndef test_visible():\n    rows = [{'rank': 82, 'value': 'b'}, {'rank': 81, 'value': 'z'}, {'rank': 81, 'value': 'a'}]\n    assert arrange_10(rows) == [rows[1], rows[2], rows[0]]\n    assert rows[0]['rank'] == 82\n    assert arrange_10([]) == []\n"
  },
  "paired_sort_11": {
    "component_9951fb07.py": "def arrange_11(records):\n    keys = sorted((row[0], row[1]) for row in records)\n    paired = sorted([((row[0], row[1]), row[2]) for row in records], key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_9951fb07 import arrange_11\n\ndef test_visible():\n    rows = [('b', 88, 'x'), ('a', 89, 'z'), ('a', 88, 'm'), ('a', 88, 'a')]\n    assert arrange_11(rows) == [(('a', 88), 'm'), (('a', 88), 'a'), (('a', 89), 'z'), (('b', 88), 'x')]\n    assert arrange_11([]) == []\n"
  },
  "paired_sort_12": {
    "component_5efbbf90.py": "def arrange_12(labels, payloads):\n    if len(labels) != len(payloads):\n        raise ValueError('length mismatch')\n    paired = sorted(zip(labels, payloads), key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_5efbbf90 import arrange_12\n\ndef test_visible():\n    assert arrange_12([97, 95, 96], ['b', 'z', 'a']) == [(95, 'z'), (96, 'a'), (97, 'b')]\n    assert arrange_12([95, 95], ['z', 'a']) == [(95, 'z'), (95, 'a')]\n    assert arrange_12([], []) == []\n"
  },
  "paired_sort_13": {
    "component_17120949.py": "def arrange_13(records):\n    ranks = sorted(row['rank'] for row in records)\n    selected = sorted(records, key=lambda row: row['rank'])\n    return selected\n",
    "test_visible.py": "from component_17120949 import arrange_13\n\ndef test_visible():\n    rows = [{'rank': 103, 'value': 'b'}, {'rank': 102, 'value': 'z'}, {'rank': 102, 'value': 'a'}]\n    assert arrange_13(rows) == [rows[1], rows[2], rows[0]]\n    assert rows[0]['rank'] == 103\n    assert arrange_13([]) == []\n"
  },
  "paired_sort_14": {
    "component_50008134.py": "def arrange_14(records):\n    keys = sorted((row[0], row[1]) for row in records)\n    paired = sorted([((row[0], row[1]), row[2]) for row in records], key=lambda item: item[0])\n    return list(paired)\n",
    "test_visible.py": "from component_50008134 import arrange_14\n\ndef test_visible():\n    rows = [('b', 109, 'x'), ('a', 110, 'z'), ('a', 109, 'm'), ('a', 109, 'a')]\n    assert arrange_14(rows) == [(('a', 109), 'm'), (('a', 109), 'a'), (('a', 110), 'z'), (('b', 109), 'x')]\n    assert arrange_14([]) == []\n"
  },
  "versioned_cache_00": {
    "component_99f9e170.py": "class Memo_0:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, item, revision):\n        return (item, revision)\n\n    def calculate(self, item, revision, values):\n        key = self.identity(item, revision)\n        if key not in self.entries:\n            self.entries[key] = sum(values)\n        return self.entries[key]\n",
    "test_visible.py": "from component_99f9e170 import Memo_0\n\ndef test_visible():\n    m = Memo_0()\n    assert m.calculate('record', 11, [3, 5]) == 8\n    assert m.calculate('record', 12, [7, 9]) == 16\n    assert m.calculate('record', 11, [100]) == 8\n    assert m.calculate('other', 11, [2, 3]) == 5\n"
  },
  "versioned_cache_01": {
    "component_23180ec4.py": "class Memo_1:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, text, mode):\n        return (text, mode)\n\n    def render(self, text, mode):\n        key = self.identity(text, mode)\n        if key not in self.entries:\n            self.entries[key] = text.lower() if mode == 'lower' else text.upper()\n        return self.entries[key]\n",
    "test_visible.py": "from component_23180ec4 import Memo_1\n\ndef test_visible():\n    m = Memo_1()\n    assert m.render('AbC18', 'lower') == 'abc18'\n    assert m.render('AbC18', 'upper') == 'ABC18'\n    assert m.render('AbC18', 'lower') == 'abc18'\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_02": {
    "component_f223991b.py": "class Memo_2:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, account, tenant):\n        return (account, tenant)\n\n    def balance(self, account, tenant, entries):\n        key = self.identity(account, tenant)\n        if key not in self.entries:\n            self.entries[key] = sum(entries)\n        return self.entries[key]\n",
    "test_visible.py": "from component_f223991b import Memo_2\n\ndef test_visible():\n    m = Memo_2()\n    assert m.balance('user', 'east', [25, 3]) == 28\n    assert m.balance('user', 'west', [33, 5]) == 38\n    assert m.balance('user', 'east', [9000]) == 28\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_03": {
    "component_b9aa1cb1.py": "class Memo_3:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, item, revision):\n        return (item, revision)\n\n    def calculate(self, item, revision, values):\n        key = self.identity(item, revision)\n        if key not in self.entries:\n            self.entries[key] = sum(values)\n        return self.entries[key]\n",
    "test_visible.py": "from component_b9aa1cb1 import Memo_3\n\ndef test_visible():\n    m = Memo_3()\n    assert m.calculate('record', 32, [3, 5]) == 8\n    assert m.calculate('record', 33, [7, 9]) == 16\n    assert m.calculate('record', 32, [100]) == 8\n    assert m.calculate('other', 32, [2, 3]) == 5\n"
  },
  "versioned_cache_04": {
    "component_ad5952d9.py": "class Memo_4:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, text, mode):\n        return (text, mode)\n\n    def render(self, text, mode):\n        key = self.identity(text, mode)\n        if key not in self.entries:\n            self.entries[key] = text.lower() if mode == 'lower' else text.upper()\n        return self.entries[key]\n",
    "test_visible.py": "from component_ad5952d9 import Memo_4\n\ndef test_visible():\n    m = Memo_4()\n    assert m.render('AbC39', 'lower') == 'abc39'\n    assert m.render('AbC39', 'upper') == 'ABC39'\n    assert m.render('AbC39', 'lower') == 'abc39'\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_05": {
    "component_70616ed7.py": "class Memo_5:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, account, tenant):\n        return (account, tenant)\n\n    def balance(self, account, tenant, entries):\n        key = self.identity(account, tenant)\n        if key not in self.entries:\n            self.entries[key] = sum(entries)\n        return self.entries[key]\n",
    "test_visible.py": "from component_70616ed7 import Memo_5\n\ndef test_visible():\n    m = Memo_5()\n    assert m.balance('user', 'east', [46, 3]) == 49\n    assert m.balance('user', 'west', [54, 5]) == 59\n    assert m.balance('user', 'east', [9000]) == 49\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_06": {
    "component_acb5570f.py": "class Memo_6:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, item, revision):\n        return (item, revision)\n\n    def calculate(self, item, revision, values):\n        key = self.identity(item, revision)\n        if key not in self.entries:\n            self.entries[key] = sum(values)\n        return self.entries[key]\n",
    "test_visible.py": "from component_acb5570f import Memo_6\n\ndef test_visible():\n    m = Memo_6()\n    assert m.calculate('record', 53, [3, 5]) == 8\n    assert m.calculate('record', 54, [7, 9]) == 16\n    assert m.calculate('record', 53, [100]) == 8\n    assert m.calculate('other', 53, [2, 3]) == 5\n"
  },
  "versioned_cache_07": {
    "component_2a8d8ed6.py": "class Memo_7:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, text, mode):\n        return (text, mode)\n\n    def render(self, text, mode):\n        key = self.identity(text, mode)\n        if key not in self.entries:\n            self.entries[key] = text.lower() if mode == 'lower' else text.upper()\n        return self.entries[key]\n",
    "test_visible.py": "from component_2a8d8ed6 import Memo_7\n\ndef test_visible():\n    m = Memo_7()\n    assert m.render('AbC60', 'lower') == 'abc60'\n    assert m.render('AbC60', 'upper') == 'ABC60'\n    assert m.render('AbC60', 'lower') == 'abc60'\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_08": {
    "component_3540316b.py": "class Memo_8:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, account, tenant):\n        return (account, tenant)\n\n    def balance(self, account, tenant, entries):\n        key = self.identity(account, tenant)\n        if key not in self.entries:\n            self.entries[key] = sum(entries)\n        return self.entries[key]\n",
    "test_visible.py": "from component_3540316b import Memo_8\n\ndef test_visible():\n    m = Memo_8()\n    assert m.balance('user', 'east', [67, 3]) == 70\n    assert m.balance('user', 'west', [75, 5]) == 80\n    assert m.balance('user', 'east', [9000]) == 70\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_09": {
    "component_58cda93e.py": "class Memo_9:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, item, revision):\n        return (item, revision)\n\n    def calculate(self, item, revision, values):\n        key = self.identity(item, revision)\n        if key not in self.entries:\n            self.entries[key] = sum(values)\n        return self.entries[key]\n",
    "test_visible.py": "from component_58cda93e import Memo_9\n\ndef test_visible():\n    m = Memo_9()\n    assert m.calculate('record', 74, [3, 5]) == 8\n    assert m.calculate('record', 75, [7, 9]) == 16\n    assert m.calculate('record', 74, [100]) == 8\n    assert m.calculate('other', 74, [2, 3]) == 5\n"
  },
  "versioned_cache_10": {
    "component_203cc819.py": "class Memo_10:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, text, mode):\n        return (text, mode)\n\n    def render(self, text, mode):\n        key = self.identity(text, mode)\n        if key not in self.entries:\n            self.entries[key] = text.lower() if mode == 'lower' else text.upper()\n        return self.entries[key]\n",
    "test_visible.py": "from component_203cc819 import Memo_10\n\ndef test_visible():\n    m = Memo_10()\n    assert m.render('AbC81', 'lower') == 'abc81'\n    assert m.render('AbC81', 'upper') == 'ABC81'\n    assert m.render('AbC81', 'lower') == 'abc81'\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_11": {
    "component_fa727e75.py": "class Memo_11:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, account, tenant):\n        return (account, tenant)\n\n    def balance(self, account, tenant, entries):\n        key = self.identity(account, tenant)\n        if key not in self.entries:\n            self.entries[key] = sum(entries)\n        return self.entries[key]\n",
    "test_visible.py": "from component_fa727e75 import Memo_11\n\ndef test_visible():\n    m = Memo_11()\n    assert m.balance('user', 'east', [88, 3]) == 91\n    assert m.balance('user', 'west', [96, 5]) == 101\n    assert m.balance('user', 'east', [9000]) == 91\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_12": {
    "component_84237aed.py": "class Memo_12:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, item, revision):\n        return (item, revision)\n\n    def calculate(self, item, revision, values):\n        key = self.identity(item, revision)\n        if key not in self.entries:\n            self.entries[key] = sum(values)\n        return self.entries[key]\n",
    "test_visible.py": "from component_84237aed import Memo_12\n\ndef test_visible():\n    m = Memo_12()\n    assert m.calculate('record', 95, [3, 5]) == 8\n    assert m.calculate('record', 96, [7, 9]) == 16\n    assert m.calculate('record', 95, [100]) == 8\n    assert m.calculate('other', 95, [2, 3]) == 5\n"
  },
  "versioned_cache_13": {
    "component_9423eee3.py": "class Memo_13:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, text, mode):\n        return (text, mode)\n\n    def render(self, text, mode):\n        key = self.identity(text, mode)\n        if key not in self.entries:\n            self.entries[key] = text.lower() if mode == 'lower' else text.upper()\n        return self.entries[key]\n",
    "test_visible.py": "from component_9423eee3 import Memo_13\n\ndef test_visible():\n    m = Memo_13()\n    assert m.render('AbC102', 'lower') == 'abc102'\n    assert m.render('AbC102', 'upper') == 'ABC102'\n    assert m.render('AbC102', 'lower') == 'abc102'\n    assert len(m.entries) == 2\n"
  },
  "versioned_cache_14": {
    "component_2852f255.py": "class Memo_14:\n    def __init__(self):\n        self.entries = {}\n\n    def identity(self, account, tenant):\n        return (account, tenant)\n\n    def balance(self, account, tenant, entries):\n        key = self.identity(account, tenant)\n        if key not in self.entries:\n            self.entries[key] = sum(entries)\n        return self.entries[key]\n",
    "test_visible.py": "from component_2852f255 import Memo_14\n\ndef test_visible():\n    m = Memo_14()\n    assert m.balance('user', 'east', [109, 3]) == 112\n    assert m.balance('user', 'west', [117, 5]) == 122\n    assert m.balance('user', 'east', [9000]) == 112\n    assert len(m.entries) == 2\n"
  },
  "snapshot_alias_00": {
    "component_2968ac70.py": "class Journal_0:\n    def __init__(self, values):\n        self.live = list(values)\n        self.checkpoint = self.live.copy()\n\n    def replace(self, index, value):\n        self.live[index] = value\n\n    def totals(self):\n        return (sum(self.live), sum(self.checkpoint))\n",
    "test_visible.py": "from component_2968ac70 import Journal_0\n\ndef test_visible():\n    j = Journal_0([11, 3, 5])\n    assert j.totals() == (19, 19)\n    j.replace(1, 9)\n    assert j.totals() == (25, 19)\n    j.replace(0, 0)\n    assert j.totals() == (14, 19)\n"
  },
  "snapshot_alias_01": {
    "component_f9ce7169.py": "class Journal_1:\n    def __init__(self, settings):\n        self.live = dict(settings)\n        self.checkpoint = self.live.copy()\n\n    def put(self, key, value):\n        self.live[key] = value\n\n    def value(self, key):\n        return (self.live[key], self.checkpoint[key])\n",
    "test_visible.py": "from component_f9ce7169 import Journal_1\n\ndef test_visible():\n    j = Journal_1({'limit': 18, 'rate': 3})\n    j.put('limit', 0)\n    assert j.value('limit') == (0, 18)\n    j.put('rate', 23)\n    assert j.value('rate') == (23, 3)\n"
  },
  "snapshot_alias_02": {
    "component_11d65f3d.py": "class Journal_2:\n    def __init__(self, rows):\n        self.live = [list(row) for row in rows]\n        self.checkpoint = [row.copy() for row in self.live]\n\n    def replace(self, row, column, value):\n        self.live[row][column] = value\n\n    def totals(self):\n        return (sum(map(sum, self.live)), sum(map(sum, self.checkpoint)))\n",
    "test_visible.py": "from component_11d65f3d import Journal_2\n\ndef test_visible():\n    j = Journal_2([[25, 3], [5, 7]])\n    j.replace(0, 1, 0)\n    assert j.totals() == (37, 40)\n    j.replace(1, 0, 9)\n    assert j.totals() == (41, 40)\n"
  },
  "snapshot_alias_03": {
    "component_a708c2c4.py": "class Journal_3:\n    def __init__(self, values):\n        self.live = list(values)\n        self.checkpoint = self.live.copy()\n\n    def replace(self, index, value):\n        self.live[index] = value\n\n    def totals(self):\n        return (sum(self.live), sum(self.checkpoint))\n",
    "test_visible.py": "from component_a708c2c4 import Journal_3\n\ndef test_visible():\n    j = Journal_3([32, 3, 5])\n    assert j.totals() == (40, 40)\n    j.replace(1, 9)\n    assert j.totals() == (46, 40)\n    j.replace(0, 0)\n    assert j.totals() == (14, 40)\n"
  },
  "snapshot_alias_04": {
    "component_6ff94b33.py": "class Journal_4:\n    def __init__(self, settings):\n        self.live = dict(settings)\n        self.checkpoint = self.live.copy()\n\n    def put(self, key, value):\n        self.live[key] = value\n\n    def value(self, key):\n        return (self.live[key], self.checkpoint[key])\n",
    "test_visible.py": "from component_6ff94b33 import Journal_4\n\ndef test_visible():\n    j = Journal_4({'limit': 39, 'rate': 3})\n    j.put('limit', 0)\n    assert j.value('limit') == (0, 39)\n    j.put('rate', 44)\n    assert j.value('rate') == (44, 3)\n"
  },
  "snapshot_alias_05": {
    "component_d487433a.py": "class Journal_5:\n    def __init__(self, rows):\n        self.live = [list(row) for row in rows]\n        self.checkpoint = [row.copy() for row in self.live]\n\n    def replace(self, row, column, value):\n        self.live[row][column] = value\n\n    def totals(self):\n        return (sum(map(sum, self.live)), sum(map(sum, self.checkpoint)))\n",
    "test_visible.py": "from component_d487433a import Journal_5\n\ndef test_visible():\n    j = Journal_5([[46, 3], [5, 7]])\n    j.replace(0, 1, 0)\n    assert j.totals() == (58, 61)\n    j.replace(1, 0, 9)\n    assert j.totals() == (62, 61)\n"
  },
  "snapshot_alias_06": {
    "component_febe3a64.py": "class Journal_6:\n    def __init__(self, values):\n        self.live = list(values)\n        self.checkpoint = self.live.copy()\n\n    def replace(self, index, value):\n        self.live[index] = value\n\n    def totals(self):\n        return (sum(self.live), sum(self.checkpoint))\n",
    "test_visible.py": "from component_febe3a64 import Journal_6\n\ndef test_visible():\n    j = Journal_6([53, 3, 5])\n    assert j.totals() == (61, 61)\n    j.replace(1, 9)\n    assert j.totals() == (67, 61)\n    j.replace(0, 0)\n    assert j.totals() == (14, 61)\n"
  },
  "snapshot_alias_07": {
    "component_c89699f6.py": "class Journal_7:\n    def __init__(self, settings):\n        self.live = dict(settings)\n        self.checkpoint = self.live.copy()\n\n    def put(self, key, value):\n        self.live[key] = value\n\n    def value(self, key):\n        return (self.live[key], self.checkpoint[key])\n",
    "test_visible.py": "from component_c89699f6 import Journal_7\n\ndef test_visible():\n    j = Journal_7({'limit': 60, 'rate': 3})\n    j.put('limit', 0)\n    assert j.value('limit') == (0, 60)\n    j.put('rate', 65)\n    assert j.value('rate') == (65, 3)\n"
  },
  "snapshot_alias_08": {
    "component_890b6913.py": "class Journal_8:\n    def __init__(self, rows):\n        self.live = [list(row) for row in rows]\n        self.checkpoint = [row.copy() for row in self.live]\n\n    def replace(self, row, column, value):\n        self.live[row][column] = value\n\n    def totals(self):\n        return (sum(map(sum, self.live)), sum(map(sum, self.checkpoint)))\n",
    "test_visible.py": "from component_890b6913 import Journal_8\n\ndef test_visible():\n    j = Journal_8([[67, 3], [5, 7]])\n    j.replace(0, 1, 0)\n    assert j.totals() == (79, 82)\n    j.replace(1, 0, 9)\n    assert j.totals() == (83, 82)\n"
  },
  "snapshot_alias_09": {
    "component_034c69ab.py": "class Journal_9:\n    def __init__(self, values):\n        self.live = list(values)\n        self.checkpoint = self.live.copy()\n\n    def replace(self, index, value):\n        self.live[index] = value\n\n    def totals(self):\n        return (sum(self.live), sum(self.checkpoint))\n",
    "test_visible.py": "from component_034c69ab import Journal_9\n\ndef test_visible():\n    j = Journal_9([74, 3, 5])\n    assert j.totals() == (82, 82)\n    j.replace(1, 9)\n    assert j.totals() == (88, 82)\n    j.replace(0, 0)\n    assert j.totals() == (14, 82)\n"
  },
  "snapshot_alias_10": {
    "component_8fbb1401.py": "class Journal_10:\n    def __init__(self, settings):\n        self.live = dict(settings)\n        self.checkpoint = self.live.copy()\n\n    def put(self, key, value):\n        self.live[key] = value\n\n    def value(self, key):\n        return (self.live[key], self.checkpoint[key])\n",
    "test_visible.py": "from component_8fbb1401 import Journal_10\n\ndef test_visible():\n    j = Journal_10({'limit': 81, 'rate': 3})\n    j.put('limit', 0)\n    assert j.value('limit') == (0, 81)\n    j.put('rate', 86)\n    assert j.value('rate') == (86, 3)\n"
  },
  "snapshot_alias_11": {
    "component_bae5cfcc.py": "class Journal_11:\n    def __init__(self, rows):\n        self.live = [list(row) for row in rows]\n        self.checkpoint = [row.copy() for row in self.live]\n\n    def replace(self, row, column, value):\n        self.live[row][column] = value\n\n    def totals(self):\n        return (sum(map(sum, self.live)), sum(map(sum, self.checkpoint)))\n",
    "test_visible.py": "from component_bae5cfcc import Journal_11\n\ndef test_visible():\n    j = Journal_11([[88, 3], [5, 7]])\n    j.replace(0, 1, 0)\n    assert j.totals() == (100, 103)\n    j.replace(1, 0, 9)\n    assert j.totals() == (104, 103)\n"
  },
  "snapshot_alias_12": {
    "component_183a6d6a.py": "class Journal_12:\n    def __init__(self, values):\n        self.live = list(values)\n        self.checkpoint = self.live.copy()\n\n    def replace(self, index, value):\n        self.live[index] = value\n\n    def totals(self):\n        return (sum(self.live), sum(self.checkpoint))\n",
    "test_visible.py": "from component_183a6d6a import Journal_12\n\ndef test_visible():\n    j = Journal_12([95, 3, 5])\n    assert j.totals() == (103, 103)\n    j.replace(1, 9)\n    assert j.totals() == (109, 103)\n    j.replace(0, 0)\n    assert j.totals() == (14, 103)\n"
  },
  "snapshot_alias_13": {
    "component_83f0f0c9.py": "class Journal_13:\n    def __init__(self, settings):\n        self.live = dict(settings)\n        self.checkpoint = self.live.copy()\n\n    def put(self, key, value):\n        self.live[key] = value\n\n    def value(self, key):\n        return (self.live[key], self.checkpoint[key])\n",
    "test_visible.py": "from component_83f0f0c9 import Journal_13\n\ndef test_visible():\n    j = Journal_13({'limit': 102, 'rate': 3})\n    j.put('limit', 0)\n    assert j.value('limit') == (0, 102)\n    j.put('rate', 107)\n    assert j.value('rate') == (107, 3)\n"
  },
  "snapshot_alias_14": {
    "component_ebd718c9.py": "class Journal_14:\n    def __init__(self, rows):\n        self.live = [list(row) for row in rows]\n        self.checkpoint = [row.copy() for row in self.live]\n\n    def replace(self, row, column, value):\n        self.live[row][column] = value\n\n    def totals(self):\n        return (sum(map(sum, self.live)), sum(map(sum, self.checkpoint)))\n",
    "test_visible.py": "from component_ebd718c9 import Journal_14\n\ndef test_visible():\n    j = Journal_14([[109, 3], [5, 7]])\n    j.replace(0, 1, 0)\n    assert j.totals() == (121, 124)\n    j.replace(1, 0, 9)\n    assert j.totals() == (125, 124)\n"
  },
  "explicit_zero_00": {
    "component_1c8587c6.py": "def configure_0(defaults, overrides):\n    selected = {key: value for key, value in overrides.items() if value is not None}\n    combined = defaults.copy()\n    combined.update(selected)\n    return combined\n",
    "test_visible.py": "from component_1c8587c6 import configure_0\n\ndef test_visible():\n    defaults = {'quota': 11, 'enabled': True, 'label': 'old', 'rows': [1], 'keep': 7}\n    overrides = {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': None}\n    assert configure_0(defaults, overrides) == {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': 7}\n    assert defaults['quota'] == 11\n    assert overrides['keep'] is None\n"
  },
  "explicit_zero_01": {
    "component_8c8906db.py": "def configure_1(original, incoming):\n    value = incoming.get('limit')\n    chosen = original if value is None else value\n    return chosen\n",
    "test_visible.py": "from component_8c8906db import configure_1\n\ndef test_visible():\n    assert configure_1(18, {'limit': 0}) == 0\n    assert configure_1(18, {'limit': False}) is False\n    assert configure_1(18, {'limit': ''}) == ''\n    assert configure_1(18, {'limit': None}) == 18\n    assert configure_1(18, {}) == 18\n"
  },
  "explicit_zero_02": {
    "component_ce48b75c.py": "def configure_2(defaults, layers):\n    chosen = {key: value for layer in layers for key, value in layer.items() if value is not None}\n    output = defaults.copy()\n    output.update(chosen)\n    return output\n",
    "test_visible.py": "from component_ce48b75c import configure_2\n\ndef test_visible():\n    base = {'limit': 25, 'flag': True}\n    layers = [{'limit': 26, 'flag': False}, {'limit': 0}, {'limit': None}]\n    assert configure_2(base, layers) == {'limit': 0, 'flag': False}\n    assert base['limit'] == 25\n    assert layers[1]['limit'] == 0\n"
  },
  "explicit_zero_03": {
    "component_be0e0fa0.py": "def configure_3(defaults, overrides):\n    selected = {key: value for key, value in overrides.items() if value is not None}\n    combined = defaults.copy()\n    combined.update(selected)\n    return combined\n",
    "test_visible.py": "from component_be0e0fa0 import configure_3\n\ndef test_visible():\n    defaults = {'quota': 32, 'enabled': True, 'label': 'old', 'rows': [1], 'keep': 7}\n    overrides = {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': None}\n    assert configure_3(defaults, overrides) == {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': 7}\n    assert defaults['quota'] == 32\n    assert overrides['keep'] is None\n"
  },
  "explicit_zero_04": {
    "component_055809dc.py": "def configure_4(original, incoming):\n    value = incoming.get('limit')\n    chosen = original if value is None else value\n    return chosen\n",
    "test_visible.py": "from component_055809dc import configure_4\n\ndef test_visible():\n    assert configure_4(39, {'limit': 0}) == 0\n    assert configure_4(39, {'limit': False}) is False\n    assert configure_4(39, {'limit': ''}) == ''\n    assert configure_4(39, {'limit': None}) == 39\n    assert configure_4(39, {}) == 39\n"
  },
  "explicit_zero_05": {
    "component_94196193.py": "def configure_5(defaults, layers):\n    chosen = {key: value for layer in layers for key, value in layer.items() if value is not None}\n    output = defaults.copy()\n    output.update(chosen)\n    return output\n",
    "test_visible.py": "from component_94196193 import configure_5\n\ndef test_visible():\n    base = {'limit': 46, 'flag': True}\n    layers = [{'limit': 47, 'flag': False}, {'limit': 0}, {'limit': None}]\n    assert configure_5(base, layers) == {'limit': 0, 'flag': False}\n    assert base['limit'] == 46\n    assert layers[1]['limit'] == 0\n"
  },
  "explicit_zero_06": {
    "component_b37c7290.py": "def configure_6(defaults, overrides):\n    selected = {key: value for key, value in overrides.items() if value is not None}\n    combined = defaults.copy()\n    combined.update(selected)\n    return combined\n",
    "test_visible.py": "from component_b37c7290 import configure_6\n\ndef test_visible():\n    defaults = {'quota': 53, 'enabled': True, 'label': 'old', 'rows': [1], 'keep': 7}\n    overrides = {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': None}\n    assert configure_6(defaults, overrides) == {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': 7}\n    assert defaults['quota'] == 53\n    assert overrides['keep'] is None\n"
  },
  "explicit_zero_07": {
    "component_442eefee.py": "def configure_7(original, incoming):\n    value = incoming.get('limit')\n    chosen = original if value is None else value\n    return chosen\n",
    "test_visible.py": "from component_442eefee import configure_7\n\ndef test_visible():\n    assert configure_7(60, {'limit': 0}) == 0\n    assert configure_7(60, {'limit': False}) is False\n    assert configure_7(60, {'limit': ''}) == ''\n    assert configure_7(60, {'limit': None}) == 60\n    assert configure_7(60, {}) == 60\n"
  },
  "explicit_zero_08": {
    "component_f9fbe4c3.py": "def configure_8(defaults, layers):\n    chosen = {key: value for layer in layers for key, value in layer.items() if value is not None}\n    output = defaults.copy()\n    output.update(chosen)\n    return output\n",
    "test_visible.py": "from component_f9fbe4c3 import configure_8\n\ndef test_visible():\n    base = {'limit': 67, 'flag': True}\n    layers = [{'limit': 68, 'flag': False}, {'limit': 0}, {'limit': None}]\n    assert configure_8(base, layers) == {'limit': 0, 'flag': False}\n    assert base['limit'] == 67\n    assert layers[1]['limit'] == 0\n"
  },
  "explicit_zero_09": {
    "component_5a08c615.py": "def configure_9(defaults, overrides):\n    selected = {key: value for key, value in overrides.items() if value is not None}\n    combined = defaults.copy()\n    combined.update(selected)\n    return combined\n",
    "test_visible.py": "from component_5a08c615 import configure_9\n\ndef test_visible():\n    defaults = {'quota': 74, 'enabled': True, 'label': 'old', 'rows': [1], 'keep': 7}\n    overrides = {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': None}\n    assert configure_9(defaults, overrides) == {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': 7}\n    assert defaults['quota'] == 74\n    assert overrides['keep'] is None\n"
  },
  "explicit_zero_10": {
    "component_d879ac11.py": "def configure_10(original, incoming):\n    value = incoming.get('limit')\n    chosen = original if value is None else value\n    return chosen\n",
    "test_visible.py": "from component_d879ac11 import configure_10\n\ndef test_visible():\n    assert configure_10(81, {'limit': 0}) == 0\n    assert configure_10(81, {'limit': False}) is False\n    assert configure_10(81, {'limit': ''}) == ''\n    assert configure_10(81, {'limit': None}) == 81\n    assert configure_10(81, {}) == 81\n"
  },
  "explicit_zero_11": {
    "component_4ff30b59.py": "def configure_11(defaults, layers):\n    chosen = {key: value for layer in layers for key, value in layer.items() if value is not None}\n    output = defaults.copy()\n    output.update(chosen)\n    return output\n",
    "test_visible.py": "from component_4ff30b59 import configure_11\n\ndef test_visible():\n    base = {'limit': 88, 'flag': True}\n    layers = [{'limit': 89, 'flag': False}, {'limit': 0}, {'limit': None}]\n    assert configure_11(base, layers) == {'limit': 0, 'flag': False}\n    assert base['limit'] == 88\n    assert layers[1]['limit'] == 0\n"
  },
  "explicit_zero_12": {
    "component_23966930.py": "def configure_12(defaults, overrides):\n    selected = {key: value for key, value in overrides.items() if value is not None}\n    combined = defaults.copy()\n    combined.update(selected)\n    return combined\n",
    "test_visible.py": "from component_23966930 import configure_12\n\ndef test_visible():\n    defaults = {'quota': 95, 'enabled': True, 'label': 'old', 'rows': [1], 'keep': 7}\n    overrides = {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': None}\n    assert configure_12(defaults, overrides) == {'quota': 0, 'enabled': False, 'label': '', 'rows': [], 'keep': 7}\n    assert defaults['quota'] == 95\n    assert overrides['keep'] is None\n"
  },
  "explicit_zero_13": {
    "component_8aecf0fe.py": "def configure_13(original, incoming):\n    value = incoming.get('limit')\n    chosen = original if value is None else value\n    return chosen\n",
    "test_visible.py": "from component_8aecf0fe import configure_13\n\ndef test_visible():\n    assert configure_13(102, {'limit': 0}) == 0\n    assert configure_13(102, {'limit': False}) is False\n    assert configure_13(102, {'limit': ''}) == ''\n    assert configure_13(102, {'limit': None}) == 102\n    assert configure_13(102, {}) == 102\n"
  },
  "explicit_zero_14": {
    "component_b35d1743.py": "def configure_14(defaults, layers):\n    chosen = {key: value for layer in layers for key, value in layer.items() if value is not None}\n    output = defaults.copy()\n    output.update(chosen)\n    return output\n",
    "test_visible.py": "from component_b35d1743 import configure_14\n\ndef test_visible():\n    base = {'limit': 109, 'flag': True}\n    layers = [{'limit': 110, 'flag': False}, {'limit': 0}, {'limit': None}]\n    assert configure_14(base, layers) == {'limit': 0, 'flag': False}\n    assert base['limit'] == 109\n    assert layers[1]['limit'] == 0\n"
  },
  "single_pass_00": {
    "component_c7c12d90.py": "def summarize_0(stream):\n    observed = tuple(stream)\n    count = len(observed)\n    total = sum(observed)\n    return {'count': count, 'total': total}\n",
    "test_visible.py": "from component_c7c12d90 import summarize_0\n\ndef test_visible():\n    assert summarize_0(iter([11, -2, 5])) == {'count': 3, 'total': 14}\n    assert summarize_0(iter([])) == {'count': 0, 'total': 0}\n    assert summarize_0(x for x in [-11, 11, 1]) == {'count': 3, 'total': 1}\n"
  },
  "single_pass_01": {
    "component_8b8051fd.py": "def summarize_1(stream):\n    observed = list(stream)\n    if any(value < 0 for value in observed):\n        raise ValueError('negative value')\n    accepted = tuple(observed)\n    return accepted\n",
    "test_visible.py": "from component_8b8051fd import summarize_1\n\ndef test_visible():\n    assert summarize_1(iter([18, 0, 5])) == (18, 0, 5)\n    assert summarize_1(iter([])) == ()\n    try:\n        summarize_1(iter([1, -1]))\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('negative accepted')\n"
  },
  "single_pass_02": {
    "component_3e5b1c0f.py": "def summarize_2(stream):\n    records = tuple(stream)\n    keys = [row[0] for row in records]\n    values = [row[1] for row in records]\n    return dict(zip(keys, values))\n",
    "test_visible.py": "from component_3e5b1c0f import summarize_2\n\ndef test_visible():\n    assert summarize_2(iter([('a', 25), ('b', 3), ('a', 27)])) == {'a': 27, 'b': 3}\n    assert summarize_2(iter([])) == {}\n"
  },
  "single_pass_03": {
    "component_f95f3d0b.py": "def summarize_3(stream):\n    observed = tuple(stream)\n    count = len(observed)\n    total = sum(observed)\n    return {'count': count, 'total': total}\n",
    "test_visible.py": "from component_f95f3d0b import summarize_3\n\ndef test_visible():\n    assert summarize_3(iter([32, -2, 5])) == {'count': 3, 'total': 35}\n    assert summarize_3(iter([])) == {'count': 0, 'total': 0}\n    assert summarize_3(x for x in [-32, 32, 1]) == {'count': 3, 'total': 1}\n"
  },
  "single_pass_04": {
    "component_16fe6500.py": "def summarize_4(stream):\n    observed = list(stream)\n    if any(value < 0 for value in observed):\n        raise ValueError('negative value')\n    accepted = tuple(observed)\n    return accepted\n",
    "test_visible.py": "from component_16fe6500 import summarize_4\n\ndef test_visible():\n    assert summarize_4(iter([39, 0, 5])) == (39, 0, 5)\n    assert summarize_4(iter([])) == ()\n    try:\n        summarize_4(iter([1, -1]))\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('negative accepted')\n"
  },
  "single_pass_05": {
    "component_02ccec90.py": "def summarize_5(stream):\n    records = tuple(stream)\n    keys = [row[0] for row in records]\n    values = [row[1] for row in records]\n    return dict(zip(keys, values))\n",
    "test_visible.py": "from component_02ccec90 import summarize_5\n\ndef test_visible():\n    assert summarize_5(iter([('a', 46), ('b', 3), ('a', 48)])) == {'a': 48, 'b': 3}\n    assert summarize_5(iter([])) == {}\n"
  },
  "single_pass_06": {
    "component_fb9c8cdf.py": "def summarize_6(stream):\n    observed = tuple(stream)\n    count = len(observed)\n    total = sum(observed)\n    return {'count': count, 'total': total}\n",
    "test_visible.py": "from component_fb9c8cdf import summarize_6\n\ndef test_visible():\n    assert summarize_6(iter([53, -2, 5])) == {'count': 3, 'total': 56}\n    assert summarize_6(iter([])) == {'count': 0, 'total': 0}\n    assert summarize_6(x for x in [-53, 53, 1]) == {'count': 3, 'total': 1}\n"
  },
  "single_pass_07": {
    "component_b2fb325a.py": "def summarize_7(stream):\n    observed = list(stream)\n    if any(value < 0 for value in observed):\n        raise ValueError('negative value')\n    accepted = tuple(observed)\n    return accepted\n",
    "test_visible.py": "from component_b2fb325a import summarize_7\n\ndef test_visible():\n    assert summarize_7(iter([60, 0, 5])) == (60, 0, 5)\n    assert summarize_7(iter([])) == ()\n    try:\n        summarize_7(iter([1, -1]))\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('negative accepted')\n"
  },
  "single_pass_08": {
    "component_4513cf72.py": "def summarize_8(stream):\n    records = tuple(stream)\n    keys = [row[0] for row in records]\n    values = [row[1] for row in records]\n    return dict(zip(keys, values))\n",
    "test_visible.py": "from component_4513cf72 import summarize_8\n\ndef test_visible():\n    assert summarize_8(iter([('a', 67), ('b', 3), ('a', 69)])) == {'a': 69, 'b': 3}\n    assert summarize_8(iter([])) == {}\n"
  },
  "single_pass_09": {
    "component_899a2e2b.py": "def summarize_9(stream):\n    observed = tuple(stream)\n    count = len(observed)\n    total = sum(observed)\n    return {'count': count, 'total': total}\n",
    "test_visible.py": "from component_899a2e2b import summarize_9\n\ndef test_visible():\n    assert summarize_9(iter([74, -2, 5])) == {'count': 3, 'total': 77}\n    assert summarize_9(iter([])) == {'count': 0, 'total': 0}\n    assert summarize_9(x for x in [-74, 74, 1]) == {'count': 3, 'total': 1}\n"
  },
  "single_pass_10": {
    "component_2e7e0bb5.py": "def summarize_10(stream):\n    observed = list(stream)\n    if any(value < 0 for value in observed):\n        raise ValueError('negative value')\n    accepted = tuple(observed)\n    return accepted\n",
    "test_visible.py": "from component_2e7e0bb5 import summarize_10\n\ndef test_visible():\n    assert summarize_10(iter([81, 0, 5])) == (81, 0, 5)\n    assert summarize_10(iter([])) == ()\n    try:\n        summarize_10(iter([1, -1]))\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('negative accepted')\n"
  },
  "single_pass_11": {
    "component_7d10e1c7.py": "def summarize_11(stream):\n    records = tuple(stream)\n    keys = [row[0] for row in records]\n    values = [row[1] for row in records]\n    return dict(zip(keys, values))\n",
    "test_visible.py": "from component_7d10e1c7 import summarize_11\n\ndef test_visible():\n    assert summarize_11(iter([('a', 88), ('b', 3), ('a', 90)])) == {'a': 90, 'b': 3}\n    assert summarize_11(iter([])) == {}\n"
  },
  "single_pass_12": {
    "component_089c5e1f.py": "def summarize_12(stream):\n    observed = tuple(stream)\n    count = len(observed)\n    total = sum(observed)\n    return {'count': count, 'total': total}\n",
    "test_visible.py": "from component_089c5e1f import summarize_12\n\ndef test_visible():\n    assert summarize_12(iter([95, -2, 5])) == {'count': 3, 'total': 98}\n    assert summarize_12(iter([])) == {'count': 0, 'total': 0}\n    assert summarize_12(x for x in [-95, 95, 1]) == {'count': 3, 'total': 1}\n"
  },
  "single_pass_13": {
    "component_0abf5acf.py": "def summarize_13(stream):\n    observed = list(stream)\n    if any(value < 0 for value in observed):\n        raise ValueError('negative value')\n    accepted = tuple(observed)\n    return accepted\n",
    "test_visible.py": "from component_0abf5acf import summarize_13\n\ndef test_visible():\n    assert summarize_13(iter([102, 0, 5])) == (102, 0, 5)\n    assert summarize_13(iter([])) == ()\n    try:\n        summarize_13(iter([1, -1]))\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('negative accepted')\n"
  },
  "single_pass_14": {
    "component_ab2af3af.py": "def summarize_14(stream):\n    records = tuple(stream)\n    keys = [row[0] for row in records]\n    values = [row[1] for row in records]\n    return dict(zip(keys, values))\n",
    "test_visible.py": "from component_ab2af3af import summarize_14\n\ndef test_visible():\n    assert summarize_14(iter([('a', 109), ('b', 3), ('a', 111)])) == {'a': 111, 'b': 3}\n    assert summarize_14(iter([])) == {}\n"
  },
  "retention_0": {
    "retention_0.py": "def permitted(value, threshold):\n    return value <= threshold\n",
    "test_visible.py": "from retention_0 import permitted\n\ndef test_visible():\n    for threshold in [31]:\n        for delta in (-1, 0, 1):\n            value = threshold + delta\n            assert permitted(value, threshold) == (value <= threshold)\n"
  },
  "retention_1": {
    "retention_1.py": "def permitted(value, threshold):\n    return value >= threshold\n",
    "test_visible.py": "from retention_1 import permitted\n\ndef test_visible():\n    for threshold in [32]:\n        for delta in (-1, 0, 1):\n            value = threshold + delta\n            assert permitted(value, threshold) == (value >= threshold)\n"
  }
}
