# Explicitly authorized repeat — 2026-09-08

After the first attempt stopped INVALID, Billy instructed: "So run it again."
This authorizes one further live attempt with the repaired error reporting,
using the existing private GitHub secret and the original $5 total allowance.

The original report is committed by SHA-256 and its full $0.000937 conservative
charge is carried into the new spending journal before any API request. A new
request is rejected unless the combined total can cover its full reservation.
The workflow is again limited to its exact launch push and first run attempt.

This repeats the same development suite, model, output cap, proposal process,
seed, nine-arm/case schedule, and 36-call recipient allocation. The original
controller and suite bytes remain unchanged. The repeat is recorded separately,
with the original episode identifier in its manifest and anchored event ledger.
It does not erase or reopen the consumed original episode.

Because this is a diagnostic replay of seen material, it cannot promote a lesson:
the unchanged success gate would instead report REPLAY_GATE_PASSED. REJECTED and
INVALID retain their original meanings. No confirmation or RSI claim follows
from the replay. The purpose is to run the real machinery and identify the
failure precisely, or obtain completed development evaluations if it proceeds.

Current verified model pricing remains as documented in CLOUD_TRIAL.md. There
is no change to the $5 total limit, and no new key or account charge allowance.

## Recorded result: INVALID

[Repeat 2 ran on GitHub](https://github.com/BillyMixNix/Hive/actions/runs/34201835268)
at commit `9552f61c853d0bbdf8df68723cbe3cbe7bfb1df7`.
The runner passed 107 offline tests, with 2 skips and 5 subtests, before the live step.

The proposer generated another lesson, then the first recipient stopped with
`failure_code: unexpected_output_items`. This identifies the rejected Responses
output structure: the adapter requires exactly one assistant message and permits
only message/reasoning items. The evidence does not identify the actual returned
item types, so it does not establish whether there were multiple messages,
native calls, or another item type. Do not infer a specific native-action fault.

| Measure | Repeat 2 | Both attempts combined |
| --- | ---: | ---: |
| Paid requests | 2 | 4 |
| Reported input tokens | 737 | 1477 |
| Reported output tokens | 313 | 628 |
| Conservative charge upper bound | $0.0009319 | $0.0018689 |
| Unresolved charge reservations | $0 | $0 |
| Evaluated recipient cases | 0 | 0 |
| Lessons added to ordinary guidance | 0 | 0 |

At least **$4.9981311** remains within the original $5 authorization. These are
conservative bounds calculated from reported usage, not an account invoice.

The replay ledger verified. No further paid request was made after its stop.
The original verdict remains unchanged. This identifies a transport-interface
blocker before evaluation; it supplies no measurement of a learning effect.

The downloaded artifact ZIP SHA-256 is
`0b28053c1fff37e5d915d57e248c758dad3e1f60656c0c8ee1e55459b718cd01`.
GitHub's digest and all internal evidence checksums were verified after download.
The original [report](results/2026-09-08-repeat2/report.json) and
[spending journal](results/2026-09-08-repeat2/spending.jsonl) are preserved unchanged.
