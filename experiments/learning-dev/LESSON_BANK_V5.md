# Context and explicit memory checkpoint comparison — 8 September 2026

The completed V4 comparison reported `GAIN_NOT_CONFIRMED`. The user then asked
whether to give up. After a proposal to correct incomplete repair context and
revise lesson use, the user explicitly authorized: **“Ok go ahead.”** This is one
new prospective redesign/retest. V4 and every earlier failure remain in the record.

## Revision and causal question

The original repair handoff could omit the earlier local holding a materialized
iterator. `ContextHiveExecutive` supplies the complete selected callable to the
investigator and repair worker, and includes the original public objective in the
repair handoff. It supports callables up to 160 lines / 24,000 characters and
refuses silent truncation beyond that bound. It does not expand write authority,
the selected statement, available tools, acceptance rules, or reviewer authority.
The recovered `hive_orchestrator.py` remains unchanged; the revision is a subclass.

The direct action schema requires an advisory `memory_check`: supplied memory ID
or `none`, applicability, brief public evidence, and predicted observable effect.
The complete actual provider output is retained. Its checkpoint is logged; only
the original action arguments reach the controller. The checkpoint is a model
claim, not verified reasoning, an executed test, or proof of lesson compliance.
Unknown memory IDs and malformed outputs consume a fully metered failed recipient.

All three arms receive this same context revision, prompt, schema and call budget:

1. **Baseline:** empty historical memory.
2. **Lesson:** the entire unedited V4 model-generated three-lesson bank.
3. **Neutral:** three unrelated archival notes, word-matched to lesson fields.

The comparison isolates historical content within this revised system. It does
not separately estimate the effect of context expansion or checkpoints versus
passive memory. It does not compare the new scores causally with the old task set.
No family-specific memory routing, human-written repair hint, learned executable
guard, extra model planner, or changed model weights is introduced.

## Frozen memory and sample

Bank SHA-256: `53e959ee24c1c0fca0be1f899ce711f2636f01c3925ed5e9df3cd4d5cb6936a4`.
These are exactly the three lessons generated in V4 formation, before these new
fixtures existed. No new proposal, refinement, selection, or diagnostic API call
is planned. Prior V4 failure feedback is not added to the lessons.

Eighteen authored transfer projects cover six association transformations, six
iterator transformations, and six cache-identity situations. Three additional
projects cover unrelated None/zero, snapshot aliasing, and current-input behavior.
The cache cases share a key-construction skeleton; these are not 18 independent
real-world defect families. Conclusions remain conditional on these authored
tasks, this controller, and the `gpt-5.6-luna` alias.

All exact projects, independent evaluators, reference edits, and offline fixture
results are committed before any paid call. Reference edits and evaluator source
are never supplied to the recipient. Each protected candidate is hashed before
its independent evaluation. Generator/custom iterator, advanced cursor, stable
association, input preservation, and overridable key-hook checks are in the
original protected tests, not a later outcome-dependent amendment.

Each task runs once in each arm: **63 recipients** with fresh workspaces and
controllers, 36 calls each, 4,096 maximum output tokens, the same model alias.
Seed 508917 plus phase seeds the predeclared within-phase shuffle. Phases 1–3
contain six transfer tasks each (two per family); phase 4 contains three retention
tasks. No repetition, replacement, optional favorable stopping, or extension is
allowed. Every settled model failure remains an unsuccessful task. HTTP errors,
unknown usage, failed evaluation integrity, or insufficient budget stop the run
and prevent a complete positive finding.

## Decision and spending

The frozen primary decision retains V4's demanding requirements: all 18 lesson
transfer tasks and all three lesson retention tasks must pass. Mean calls to
verified repair must decrease by at least 15% against **both** controls, with an
exact one-sided paired sign-flip p ≤ 0.00625 against each. Failures score 36 for
utility only; report actual calls, tokens and spending separately. This consumes
the remaining allocation after .025 + .0125 + .00625 = .04375 across earlier
confirmatory attempts; the total nominal allocation is .05. No future allocation
or repeated significance search is authorized by this protocol.

Otherwise report `GAIN_NOT_CONFIRMED`, or `INCOMPLETE` / `INVALID` if the planned
sample or integrity is missing. Report checkpoint selections descriptively; they
cannot rescue a failed primary result or replace executable correctness evidence.

The original total API ceiling remains **$5**, with **$2.3149073** already charged
or conservatively reserved. That includes the permanent **$0.5323728** reservation
for the earlier HTTP 400. The remaining nominal allowance is **$2.6850927**; a new
request must still fit the full conservative reservation. No prior cost is reset
or refunded. Pricing verification expires at 2026-09-09 00:00 UTC. Each recipient
has an exclusive journal inheriting its predecessor's entire cumulative amount.
GitHub run-attempt and launch-parent checks prevent replay; the credential is
supplied only after offline verification through the existing Actions secret.

A positive result would support external memory use on this task distribution.
It would not establish fine-tuning, general model improvement, or RSI.
