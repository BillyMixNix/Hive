# Public contract comparison rerun

Billy authorized this run with “Ok run it again” after receiving the offline
input-contract repair. This is a new descriptive rerun, not a retry of a failed
HTTP request or a rescue of either earlier study's confirmation claim.

Run all 15 previously evaluated follow-on tasks once in each of three conditions:
no retained lessons, the same three frozen model-generated lessons, and the same
word-count-matched neutral notes. Each recipient starts fresh with the original
public files and objective and receives the original 36-call allocation. The
model remains `gpt-5.6-luna`, with default service tier, 4096 output tokens and
`store: false`. No new lesson generation, lesson editing or weight training.

The new `ContractCheckedHive` adapter checks caller-declared argument preservation
on 11 tasks. The four iterator tasks retain `IndexedCheckpointHive`: consuming
an iterator is required there, and the new checker does not support iterator
snapshots. This choice is fixed by task and identical in all three conditions.
The public checker sees only existing public tests. Original acceptance tests
remain separate. Final scoring includes the previously published full-cache audit
for all cache tasks, uniformly; these final test bodies are never model feedback.

Freeze task order and the order of the three arms within each task with seed
508918. Each task's three arms are adjacent. Freeze source hashes, the complete
study, preflight report, and the cumulative predecessor report before any paid
request. Preflight must reject every initial buggy case, accept every existing
reference repair, accept its applicable input contract, and reject all three
saved input-mutation patches. Earlier runtime and results remain unchanged.

Report all outcomes, including failed model actions and false controller
completions. Report transfer and retention separately, plus actual calls,
input/output tokens, conservative charges, paired effort differences and calls
with a 36-call penalty for each failed repair. A partial run is incomplete;
there is no substitution, favorable-task selection, inferential gain verdict,
pooling with old runs, or outcome-dependent continuation.

The original $5 total cap persists. Carry $3.4695976 before this rerun, including
the permanent earlier $0.5323728 unknown-usage reservation. The guard reserves
$0.5323728 before each network request and settles only verified usage. It may
therefore stop with some unspent headroom. Rates were rechecked on September 8,
2026 against [official pricing](https://developers.openai.com/api/docs/pricing):
the existing conservative maxima of 500 nanoUSD per input token and 1800 per
output token remain valid. Verify pricing validity before each request; stop at
2026-09-09 00:00 UTC, a 3000-second run deadline, insufficient reservation
headroom, unresolved usage, HTTP failure, or evaluation-integrity failure.
Never retry unknown requests or reset the ledger.

One GitHub push launch must match the exact planned parent, commit message,
repository, branch and run attempt 1. The existing secret is available only in
the paid step and removed from child-process environments. All requests,
responses, journals, candidate files, controller traces and final checks are
preserved. The PR stays draft and main is not merged.

These cases have already been evaluated and the gate was built after observing
their failure. Any advantage is descriptive evidence on reused authored tasks,
not proof of improvement on unseen work or recursive self-improvement.
